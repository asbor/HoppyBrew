services:
  backend:
    container_name: hoppybrew-backend-1
    build:
      context: ./services/backend
      dockerfile: Dockerfile
      args:
        USER_UID: ${HOST_UID:-1000}
        USER_GID: ${HOST_GID:-1000}
    ports:
      - 8000:8000
    volumes:
      - ./services/backend:/app
    command: sh -c "cd /app && python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000"
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - .env
    networks:
      - my-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          cpus: "0.25"
          memory: 128M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  db:
    container_name: hoppybrew-db-1
    image: postgres:latest
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=hoppybrew_db
    ports:
      - "5432:5432"
    restart: always
    networks:
      - my-network
    volumes:
      - postgres_data:/var/lib/postgresql/data
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.5"
          memory: 256M
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    container_name: hoppybrew-frontend-1
    build:
      context: ./services/nuxt3-shadcn
      dockerfile: Dockerfile
      args:
        USER_UID: ${HOST_UID:-1000}
        USER_GID: ${HOST_GID:-1000}
    ports:
      - 3000:3000
    volumes:
      - ./services/nuxt3-shadcn:/app
      - /app/node_modules
      - /app/.nuxt
    command: sh -c "yarn && yarn dev --host 0.0.0.0"
    environment:
      - API_BASE_URL=http://localhost:8000
      - HOST=0.0.0.0
    networks:
      - my-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 1G
        reservations:
          cpus: "0.5"
          memory: 256M
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

networks:
  my-network:
    driver: bridge

volumes:
  postgres_data:
