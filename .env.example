# HoppyBrew Environment Configuration
# Copy this file to .env and update the values as needed

# =============================================================================
# Application Mode
# =============================================================================
# Set to "1" to enable testing mode (uses SQLite), "0" for production (PostgreSQL)
TESTING=0

# =============================================================================
# Database Configuration (PostgreSQL)
# =============================================================================
# PostgreSQL database host (use service name from docker-compose.yml)
DATABASE_HOST=hoppybrew-db-1

# PostgreSQL database port
DATABASE_PORT=5432

# PostgreSQL database name
DATABASE_NAME=hoppybrew_db

# PostgreSQL database user
DATABASE_USER=postgres

# PostgreSQL database password (change this in production!)
DATABASE_PASSWORD=postgres

# =============================================================================
# Alembic Migration Configuration (Optional)
# =============================================================================
# Override the default database URL for Alembic migrations
# ALEMBIC_DATABASE_URL=postgresql://postgres:postgres@hoppybrew-db-1:5432/hoppybrew_db

# =============================================================================
# SQLAlchemy Configuration (Optional)
# =============================================================================
# Override the default database URL for SQLAlchemy
# SQLALCHEMY_DATABASE_URL=postgresql://postgres:postgres@hoppybrew-db-1:5432/hoppybrew_db

# =============================================================================
# Testing Configuration
# =============================================================================
# SQLite database file for testing (used when TESTING=1)
# TEST_DATABASE_URL=sqlite:///./test_hoppybrew.db

# =============================================================================
# Frontend Configuration
# =============================================================================
# API base URL for the Nuxt frontend
# 
# IMPORTANT: This value depends on your deployment environment:
# 
# For LOCAL DEVELOPMENT (both frontend and backend running outside Docker):
#   API_BASE_URL=http://localhost:8000
# 
# For DOCKER DEVELOPMENT (running with docker-compose):
#   Set in docker-compose.yml, NOT here. Frontend container uses:
#   API_BASE_URL=http://hoppybrew-backend-1:8000
#   (Uses Docker service name for container-to-container communication)
# 
# For PRODUCTION:
#   API_BASE_URL=https://api.yourdomain.com
# 
# See DEBUGGING_GUIDE.md for more information.
# API_BASE_URL=http://localhost:8000

# =============================================================================
# Docker Compose Configuration
# =============================================================================
# These values should match the database configuration above
# POSTGRES_DB=hoppybrew_db
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres

# =============================================================================
# Security & Production Settings
# =============================================================================
# TODO: Add JWT secret, session secret, CORS origins, etc. when implementing authentication
# JWT_SECRET_KEY=your-secret-key-here
# SESSION_SECRET=your-session-secret-here
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com
